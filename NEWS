Changes in extension bundle ROBMED version 0.6.0

    + Moved input for seed of the random number generator to main dialog.



Changes in extension bundle ROBMED version 0.5.0

    + Added support for multiple independent variables of interest.

    + Added support for serial multiple mediator models.



Changes in extension bundle ROBMED version 0.4.0

    + Improved handling of ordered and nominal variables, which also allows to
      use string variables as independent variable or mediators.

    + Fix in help text: added documentation of parameters that define the
      variables.
